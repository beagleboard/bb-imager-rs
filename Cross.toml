[build]
pre-build = [
    "dpkg --add-architecture $CROSS_DEB_ARCH",
    "apt-get update",
    "apt-get -y install libudev-dev:$CROSS_DEB_ARCH libssl-dev:$CROSS_DEB_ARCH libsqlite3-dev:$CROSS_DEB_ARCH liblzma-dev:$CROSS_DEB_ARCH"
]
